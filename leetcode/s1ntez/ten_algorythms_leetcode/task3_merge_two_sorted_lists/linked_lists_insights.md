# Linked Lists vs Slices/Maps:

## Linked Lists (связные списки):
```go
type ListNode struct {
    Val  int
    Next *ListNode  // +16 байт на указатель
}
```

✅ Плюсы:
    + Быстрая вставка/удаление из середины O(1)
    + Динамический размер (не нужно pre-allocate, заранее выделять память)
    + Не нужно переаллоцировать как слайсы

❌ Минусы:
    - Высокие аллокации - каждый узел = отдельная аллокация
    - Плохая cache locality - данные разбросаны по памяти
    - Медленный доступ по индексу O(n)

## Slices (слайсы):
```go
slice := make([]int, 0, 10)  // Одна аллокация!
```

✅ Плюсы:
    - Отличная cache locality - данные в contiguous memory
    - Быстрый доступ по индексу O(1)
    - Меньше аллокаций - одна аллокация на весь слайс

❌ Минусы:
    - Медленная вставка/удаление из середины O(n)
    - Может требовать reallocation при append

## 🎯 Когда что использовать:

#### Linked Lists если:
    - Частые вставки/удаления из середины
    - Неизвестный размер данных
    - Реализация структур типа LRU Cache, stacks, queues

#### Slices если:
    - Частый доступ по индексу
    - Данные последовательно обрабатываются
    - Известен примерный размер данных

### 💡 Real-world примеры:

```go
// Linked List - хорош для LRU Cache
type LRUCache struct {
    head *Node
    tail *Node
    cache map[int]*Node
}

// Slice - хорош для батч-обработки
func processUsers(users []User) {
    for i := range users {  // Быстрый sequential access
        users[i].Process()
    }
}
```