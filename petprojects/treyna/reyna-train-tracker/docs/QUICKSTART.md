# 🚀 Quick Start Guide - Рейна Трекер

Быстрое руководство по запуску и использованию проекта.

## ⚡ Моментальный старт

```bash
cd /home/denismatveev/Desktop/treyna/reyna-train-tracker
go run cmd/main.go
```

Вот и всё! Программа выведет текущую позицию Рейны и ответы на все 10 вопросов.

---

## 📁 Структура проекта

```
reyna-train-tracker/
│
├── cmd/
│   └── main.go                # 👈 НАЧНИ ОТСЮДА! Точка входа
│
├── reyna_route.json           # Данные о 88 станциях
│
├── internal/
│   ├── models/types.go        # Все структуры данных
│   ├── tracker/
│   │   ├── tracker.go         # Основная логика трекера
│   │   └── algorithms.go      # ⭐ 3 алгоритма здесь
│   ├── cache/cache.go         # In-memory кэш с RWMutex
│   ├── api/
│   │   ├── handlers.go        # ⭐ 10 вопросов здесь
│   │   ├── semaphore.go       # Паттерн: Semaphore
│   │   ├── ratelimiter.go     # Паттерн: Rate Limiter
│   │   └── loadbalancer.go    # Паттерн: Load Balancer
│   └── utils/
│       ├── timeutils.go       # Работа с временем и timezone
│       └── distance.go        # Расстояния между станциями
│
├── docs/                      # Документация
│   ├── README.md             # Полная документация
│   ├── QUICKSTART.md         # Этот файл
│   ├── TECHNICAL_DOCS.md     # Техническая документация
│   ├── ARCHITECTURE.md       # Архитектура системы
│   └── PROJECT_SUMMARY.md    # Резюме проекта
│
├── README.md                  # Основной README (быстрый старт)
└── go.mod                     # Go модуль
```

---

## 🎯 Что делает программа?

### 1. Загружает маршрут (88 станций)
```go
tracker, _ := tracker.NewTrainTracker("reyna_route.json")
```

### 2. Определяет текущую позицию Рейны
```go
position := tracker.GetCurrentPosition(time.Now())
```
- Использует алгоритм **Два указателя** (Two Pointers)
- Работает за O(n) время

### 3. Отвечает на 10 вопросов ПАРАЛЛЕЛЬНО
```go
results := handler.ProcessAllQuestions(currentTime)
```
- Использует **WaitGroup** для синхронизации
- **Fan-out**: 10 горутин обрабатывают вопросы
- **Fan-in**: Собирает результаты в один срез
- **Semaphore**: Ограничивает до 10 одновременных операций
- **Rate Limiter**: Защищает от перегрузки
- **Load Balancer**: Распределяет нагрузку между воркерами

---

## 🔍 10 вопросов

1. **Какое сейчас локальное время у пассажира?**
   - Timezone conversion с учётом всех российских часовых поясов

2. **На какой станции пассажир сейчас находится?**
   - Hash table lookup O(1)

3. **Поезд стоит или в пути?**
   - Сравнение времени прибытия/отправления

4. **Какой день путешествия?**
   - Расчёт от начала путешествия (6 октября)

5. **Какое расстояние от Москвы?**
   - Интерполяция между станциями

6. **Когда пассажир прибудет на следующую станцию?**
   - Использует **Sliding Window** для расчёта скорости

7. **Какая разница во времени между Москвой и текущим городом?**
   - Timezone offset calculation

8. **Если я пишу сейчас, когда она получит?**
   - Мгновенная доставка + timezone conversion

9. **Если она пишет сейчас, когда я получу?**
   - Обратная timezone conversion

10. **Какие основные станции впереди?**
    - Фильтрация по `IsMajor` flag

---

## 🧮 Где находятся алгоритмы?

### 1. Два указателя (Two Pointers)
**Файл**: `internal/tracker/algorithms.go`
**Функция**: `FindCurrentPositionTwoPointers()`

```go
left := 0
right := len(stations) - 1

for left <= right {
    // Ищем текущую позицию между станциями
    if currentTime между station.Arrival и station.Departure {
        return текущую_станцию
    }
    left++
}
```

### 2. Хэш-таблицы (Hash Tables)
**Файл**: `internal/tracker/algorithms.go`
**Функция**: `BuildStationHashMap()`

```go
nameMap := make(map[string]*StationInfo)
idMap := make(map[int]*StationInfo)

for _, station := range stations {
    nameMap[station.Name] = &station  // O(1) доступ по имени
    idMap[station.ID] = &station      // O(1) доступ по ID
}
```

### 3. Скользящее окно (Sliding Window)
**Файл**: `internal/tracker/algorithms.go`
**Функция**: `CalculateAverageSpeedSlidingWindow()`

```go
windowSize := 5  // Последние 5 отрезков

for i := start; i < currentIndex; i++ {
    totalDistance += distance_between_stations
    totalTime += time_between_stations
}

averageSpeed = totalDistance / totalTime
```

---

## 🔄 Где находятся паттерны конкурентности?

### WaitGroup
**Файл**: `internal/api/handlers.go`
**Функция**: `ProcessAllQuestions()`

```go
var wg sync.WaitGroup

for i := 1; i <= 10; i++ {
    wg.Add(1)
    go func(questionNum int) {
        defer wg.Done()
        // Process question
    }(i)
}

wg.Wait()  // Ждём все горутины
```

### Atomic Operations
**Файл**: `internal/tracker/tracker.go`

```go
type TrainTracker struct {
    RequestCounter   atomic.Uint64
    QuestionCounters [11]atomic.Uint64
}

tracker.RequestCounter.Add(1)  // Атомарный инкремент
```

### RWMutex
**Файл**: `internal/cache/cache.go`

```go
type InMemoryCache[T any] struct {
    data map[string]CacheEntry[T]
    mu   sync.RWMutex  // Разделяемое чтение, эксклюзивная запись
}
```

### Semaphore
**Файл**: `internal/api/semaphore.go`

```go
type Semaphore struct {
    sem chan struct{}  // Буферизованный канал
}

sem.Acquire()  // Блокируется если лимит достигнут
defer sem.Release()
```

### Rate Limiter
**Файл**: `internal/api/ratelimiter.go`

```go
type RateLimiter struct {
    tokens    int
    maxTokens int
}

if limiter.Allow() {
    // Обработать запрос
}
```

### Load Balancer
**Файл**: `internal/api/loadbalancer.go`

```go
type LoadBalancer struct {
    workers []*Worker
    next    atomic.Uint64  // Round-robin counter
}

worker := lb.GetNextWorker()  // Получаем следующего воркера
```

### Fan-in/Fan-out
**Файл**: `internal/api/handlers.go`

```go
// Fan-out: распределяем работу
for i := 1; i <= 10; i++ {
    go func() {
        result := processQuestion()
        results <- result  // Отправляем в канал
    }()
}

// Fan-in: собираем результаты
for result := range results {
    allResults = append(allResults, result)
}
```

---

## 🎓 Для преподавания ученику

### Порядок изучения:

1. **Начни с `cmd/main.go`**
   - Понять общий flow программы
   - Увидеть, как всё связано

2. **Изучи структуры данных** (`internal/models/types.go`)
   - StationInfo
   - CurrentPosition
   - CacheEntry (с generics!)

3. **Алгоритмы** (`internal/tracker/algorithms.go`)
   - Два указателя для поиска
   - Hash tables для O(1) доступа
   - Sliding window для предсказаний

4. **Кэш с RWMutex** (`internal/cache/cache.go`)
   - Generic cache implementation
   - Thread-safe с RWMutex
   - Автоматическая очистка по TTL

5. **Обработчики вопросов** (`internal/api/handlers.go`)
   - Как работает каждый из 10 вопросов
   - Fan-out/Fan-in pattern
   - WaitGroup для синхронизации

6. **Паттерны защиты** (`internal/api/`)
   - Semaphore для ограничения
   - Rate Limiter для защиты от спама
   - Load Balancer для распределения

---

## 🧪 Тестирование

### Тестирование с конкретным временем:

Замени в `cmd/main.go`:
```go
// Вместо
currentTime := time.Now()

// Используй
moscowTZ, _ := time.LoadLocation("Europe/Moscow")
currentTime := time.Date(2025, 10, 11, 10, 0, 0, 0, moscowTZ)
// 11 октября 2025, 10:00 по Москве
```

### Проверка разных станций:

```go
// 10 октября - должна быть в районе Тулуна (city_38)
currentTime := time.Date(2025, 10, 10, 12, 0, 0, 0, moscowTZ)

// 11 октября - должна быть в районе Хушенги (city_55)
currentTime := time.Date(2025, 10, 11, 10, 0, 0, 0, moscowTZ)

// 13 октября - должна быть в районе Биробиджана (city_88)
currentTime := time.Date(2025, 10, 13, 12, 0, 0, 0, moscowTZ)
```

---

## 📊 Статистика и метрики

После выполнения программа выводит статистику:

```
📊 СТАТИСТИКА ИСПОЛЬЗОВАНИЯ:
Всего запросов: 11
Размер кэша: 5 записей

Запросов по вопросам:
  Вопрос 1: 1 раз(а)
  Вопрос 2: 1 раз(а)
  ...

📊 СТАТИСТИКА LOAD BALANCER:
  Worker 1: нагрузка = 0, активен = true
  Worker 2: нагрузка = 0, активен = true
  ...

📊 RATE LIMITER: доступно токенов = 89
```

Это показывает:
- **Atomic counters** в действии
- **Load balancing** работает
- **Rate limiter** отслеживает токены

---

## 🐛 Если что-то не работает

### Проблема: "go: command not found"
**Решение**: Установи Go 1.21+
```bash
# Arch Linux
sudo pacman -S go

# Проверка
go version
```

### Проблема: "cannot find package"
**Решение**: 
```bash
cd reyna-train-tracker
go mod tidy
```

### Проблема: "файл не найден"
**Решение**: Убедись что `reyna_route.json` в той же папке
```bash
ls -la reyna_route.json
```

---

## 💡 Полезные команды

```bash
# Компиляция
go build -o reyna-tracker cmd/main.go

# Запуск с выводом статистики
go run cmd/main.go 2>&1 | tee output.log

# Проверка кода
go vet ./...

# Форматирование
go fmt ./...

# Тесты (если добавишь)
go test ./... -v
```

---

## 📚 Дополнительное чтение

- **README.md** - Полная документация проекта
- **TECHNICAL_DOCS.md** - Детальное объяснение алгоритмов и паттернов
- **Info.md** - Реальные данные о позициях Рейны
- **reyna_route.json** - Исходные данные маршрута

---

## 🎉 Готово!

Теперь у тебя есть полностью функциональная система отслеживания с:

✅ 3 алгоритмами (Two Pointers, Hash Tables, Sliding Window)
✅ 8 паттернами конкурентности (WaitGroup, Atomic, RWMutex, Semaphore, Fan-in/out, Rate Limiter, Load Balancer, Cache)
✅ Generics в Go
✅ Правильной работой с временными зонами
✅ Comprehensive документацией

**Удачи в преподавании! 🚂💨**

