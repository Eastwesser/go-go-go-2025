# 🚂 Рейна Трекер - Система отслеживания поезда Москва-Хабаровск

Комплексная система на Go для отслеживания путешествия поезда по Транссибирской магистрали с демонстрацией алгоритмов и паттернов конкурентности.

## 📋 Описание проекта

Это образовательный проект, созданный для демонстрации продвинутых концепций программирования на Go, включая:
- **Алгоритмы**: Два указателя, Хэш-таблицы, Скользящее окно
- **Паттерны конкурентности**: WaitGroup, Atomic, RWMutex, Semaphore, Fan-in/Fan-out
- **Системные паттерны**: Rate Limiter, Load Balancer, In-Memory Cache
- **Generics**: Использование параметризованных типов в Go

## 🎯 10 главных вопросов

Программа отвечает на следующие вопросы о путешествии:

1. 🕐 **Какое сейчас локальное время у пассажира?**
   - Определяет текущий часовой пояс пассажира
   - Показывает местное время с учётом всех временных зон России

2. 🏁 **На какой станции пассажир сейчас находится?**
   - Определяет текущую станцию или положение между станциями
   - Показывает расстояние от Москвы

3. 🚂 **Поезд стоит или в пути?**
   - Определяет статус поезда (на станции/в движении)
   - Показывает оставшееся время стоянки или время до следующей станции

4. 📅 **Какой день путешествия?**
   - Рассчитывает день поездки от отправления из Москвы
   - Показывает общее время в пути

5. 📏 **Какое расстояние от Москвы?**
   - Точное расстояние для текущей позиции
   - Учитывает интерполяцию между станциями

6. ⏰ **Когда пассажир прибудет на следующую станцию?**
   - Время прибытия на следующую станцию
   - Оставшееся время в пути

7. 🌍 **Какая разница во времени между Москвой и текущим городом?**
   - Разница в часах с Московским временем
   - Направление смещения (впереди/отстаёт)

8. 💬 **Если я пишу сейчас, когда она получит?**
   - Время получения сообщения с учётом временных зон
   - Мгновенная доставка

9. 💬 **Если она пишет сейчас, когда я получу?**
   - Время получения ответа в Москве
   - Учёт временной разницы

10. 🗺️ **Какие основные станции впереди и когда прибытие?**
    - Список крупных станций маршрута
    - Время прибытия и длительность стоянок

## 🏗️ Архитектура проекта

```
reyna-train-tracker/
├── cmd/
│   └── main.go                      # Точка входа в приложение
├── go.mod                           # Go модуль
├── reyna_route.json                 # Данные маршрута (88 станций)
│
├── internal/
│   ├── models/
│   │   └── types.go                 # Структуры данных проекта
│   │
│   ├── tracker/
│   │   ├── tracker.go               # Основной трекер поезда
│   │   └── algorithms.go            # Алгоритмы (2 указателя, хэш-таблицы, скользящее окно)
│   │
│   ├── cache/
│   │   └── cache.go                 # In-memory кэш с RWMutex и generics
│   │
│   ├── api/
│   │   ├── handlers.go              # Обработчики 10 вопросов
│   │   ├── semaphore.go             # Семафор для ограничения одновременных операций
│   │   ├── ratelimiter.go           # Ограничитель частоты запросов
│   │   └── loadbalancer.go          # Балансировщик нагрузки
│   │
│   └── utils/
│       ├── timeutils.go             # Утилиты работы с временем и часовыми поясами
│       └── distance.go              # Расчёты расстояний
│
├── docs/                            # Документация
│   ├── README.md                   # Этот файл (полная документация)
│   ├── QUICKSTART.md               # Быстрый старт для новичков
│   ├── TECHNICAL_DOCS.md           # Техническая документация
│   ├── ARCHITECTURE.md             # Архитектура системы
│   └── PROJECT_SUMMARY.md          # Резюме проекта
│
└── README.md                        # Основной README (в корне)
```

## 🧮 Алгоритмы

### 1. Два указателя (Two Pointers)
**Файл**: `internal/tracker/algorithms.go` → `FindCurrentPositionTwoPointers()`

**Назначение**: Определение текущей позиции пассажира между станциями в отсортированном массиве.

**Сложность**: O(n), где n - количество станций

**Как работает**:
```go
// Движемся от начала и конца к середине
left := 0
right := len(stations) - 1

// Проверяем каждую станцию и промежутки между ними
for left <= right {
    // Проверяем, на станции ли мы
    if между arrival и departure {
        return текущую_станцию
    }
    
    // Проверяем, между станциями ли мы
    if между departure предыдущей и arrival следующей {
        return позицию_между_станциями
    }
    
    left++
}
```

**Применение в проекте**: Быстрый поиск текущего положения поезда среди 88 станций.

### 2. Хэш-таблицы (Hash Tables)
**Файл**: `internal/tracker/algorithms.go` → `BuildStationHashMap()`

**Назначение**: Быстрый O(1) доступ к данным станций по названию или ID.

**Сложность**: O(1) для поиска, O(n) для построения

**Как работает**:
```go
nameMap := make(map[string]*StationInfo)
idMap := make(map[int]*StationInfo)

for _, station := range stations {
    nameMap[station.Name] = &station      // По названию
    idMap[station.ID] = &station          // По ID
}
```

**Применение в проекте**: 
- Мгновенный поиск станции по названию
- Быстрый доступ к информации о станции по ID

### 3. Скользящее окно (Sliding Window)
**Файл**: `internal/tracker/algorithms.go` → `CalculateAverageSpeedSlidingWindow()`

**Назначение**: Предсказание времени прибытия на основе средней скорости на последних N отрезках.

**Сложность**: O(k), где k - размер окна (константа)

**Как работает**:
```go
windowSize := 5  // Последние 5 отрезков
start := currentIndex - windowSize

for i := start; i < currentIndex; i++ {
    distance += расстояние_между_станциями[i]
    time += время_между_станциями[i]
}

averageSpeed = distance / time
```

**Применение в проекте**: Точное предсказание времени прибытия с учётом реальной скорости движения.

## 🔄 Паттерны конкурентности

### 1. WaitGroup
**Файл**: `internal/api/handlers.go` → `ProcessAllQuestions()`

```go
var wg sync.WaitGroup

for i := 1; i <= 10; i++ {
    wg.Add(1)
    go func(questionNum int) {
        defer wg.Done()
        // Обрабатываем вопрос
    }(i)
}

wg.Wait()  // Ждём завершения всех горутин
```

**Применение**: Параллельная обработка всех 10 вопросов одновременно.

### 2. Atomic Operations
**Файл**: `internal/tracker/tracker.go`

```go
type TrainTracker struct {
    RequestCounter   atomic.Uint64
    QuestionCounters [11]atomic.Uint64
}

tracker.RequestCounter.Add(1)           // Атомарный инкремент
count := tracker.RequestCounter.Load()  // Атомарное чтение
```

**Применение**: Потокобезопасная статистика запросов без блокировок.

### 3. RWMutex (Read-Write Mutex)
**Файл**: `internal/cache/cache.go`

```go
type InMemoryCache struct {
    data map[string]CacheEntry
    mu   sync.RWMutex
}

func (c *Cache) Get(key string) {
    c.mu.RLock()         // Разделяемая блокировка для чтения
    defer c.mu.RUnlock()
    // Читаем данные
}

func (c *Cache) Set(key string, value interface{}) {
    c.mu.Lock()          // Эксклюзивная блокировка для записи
    defer c.mu.Unlock()
    // Пишем данные
}
```

**Применение**: Эффективный concurrent доступ к кэшу (множественное чтение, эксклюзивная запись).

### 4. Semaphore
**Файл**: `internal/api/semaphore.go`

```go
type Semaphore struct {
    sem chan struct{}
}

sem := NewSemaphore(10)  // Максимум 10 одновременных операций

sem.Acquire()   // Захватить семафор
// Выполнить работу
sem.Release()   // Освободить семафор
```

**Применение**: Ограничение количества одновременных запросов к системе (защита от перегрузки).

### 5. Fan-out / Fan-in
**Файл**: `internal/api/handlers.go`

```go
// Fan-out: распределяем работу по горутинам
results := make(chan QuestionResult, 10)
for i := 1; i <= 10; i++ {
    go func(questionNum int) {
        result := processQuestion(questionNum)
        results <- result  // Отправляем результат в канал
    }(i)
}

// Fan-in: собираем результаты из канала
for result := range results {
    allResults = append(allResults, result)
}
```

**Применение**: Параллельная обработка вопросов (fan-out) и сбор результатов (fan-in).

### 6. Rate Limiter
**Файл**: `internal/api/ratelimiter.go`

```go
type RateLimiter struct {
    tokens    int
    maxTokens int
}

if limiter.Allow() {
    // Обработать запрос
} else {
    // Запрос отклонён (превышен лимит)
}
```

**Применение**: Защита от спама запросами (Token Bucket алгоритм).

### 7. Load Balancer
**Файл**: `internal/api/loadbalancer.go`

```go
type LoadBalancer struct {
    workers []*Worker
    next    atomic.Uint64
}

// Round-robin распределение
worker := lb.GetNextWorker()
worker.ProcessRequest()
lb.ReleaseWorker(worker)
```

**Применение**: Равномерное распределение нагрузки между воркерами.

### 8. In-Memory Cache with Generics
**Файл**: `internal/cache/cache.go`

```go
type Cache[T any] interface {
    Set(key string, value T, ttl time.Duration)
    Get(key string) (T, bool)
}

type InMemoryCache[T any] struct {
    data map[string]CacheEntry[T]
    mu   sync.RWMutex
}

cache := NewInMemoryCache[string]()
cache.Set("key", "value", 5*time.Minute)
```

**Применение**: Кэширование результатов вычислений с использованием generics.

## 🌍 Часовые пояса

Программа корректно обрабатывает все российские часовые пояса:

| Часовой пояс        | UTC Offset | Города                                      |
|---------------------|------------|---------------------------------------------|
| Europe/Moscow       | UTC+3      | Москва, Владимир, Нижний Новгород, Киров   |
| Asia/Yekaterinburg  | UTC+5      | Пермь, Екатеринбург, Тюмень                |
| Asia/Omsk           | UTC+6      | Омск                                        |
| Asia/Novosibirsk    | UTC+7      | Новосибирск                                 |
| Asia/Krasnoyarsk    | UTC+7      | Красноярск                                  |
| Asia/Irkutsk        | UTC+8      | Иркутск, Улан-Удэ                          |
| Asia/Yakutsk        | UTC+9      | Чита, Сковородино                          |
| Asia/Vladivostok    | UTC+10     | Хабаровск, Биробиджан                      |

## 🚀 Установка и запуск

### Требования
- Go 1.21 или выше

### Установка

```bash
# Клонируем или переходим в директорию проекта
cd reyna-train-tracker

# Устанавливаем зависимости (если есть)
go mod download

# Запускаем программу
go run cmd/main.go
```

### Компиляция

```bash
# Компилируем бинарник
go build -o reyna-tracker cmd/main.go

# Запускаем
./reyna-tracker
```

## 📊 Пример вывода

```
🚂 РЕЙНА ТРЕКЕР - Система отслеживания поезда Москва-Хабаровск
================================================================================
✅ Загружено станций: 88
📏 Общая дистанция: 9104 км
🕐 Начало путешествия: 22:10 06.10.2025

🕐 Текущее время: 10:00 11.10.2025 (Москва)
================================================================================

📍 ТЕКУЩАЯ ПОЗИЦИЯ:
--------------------------------------------------------------------------------
🚉 Станция: Хушенга
📏 Расстояние от Москвы: 6528 км
🌍 Часовой пояс: Asia/Irkutsk
🕐 Локальное время: 15:00 11.10.2025
🛑 Статус: СТОИТ НА СТАНЦИИ
⏰ Осталось стоять: 1мин

📅 ИНФОРМАЦИЯ О ПУТЕШЕСТВИИ:
   День путешествия: 5
   Время в пути: 107ч 50мин

================================================================================
🔍 ОБРАБОТКА ВСЕХ 10 ВОПРОСОВ С ИСПОЛЬЗОВАНИЕМ ПАТТЕРНОВ КОНКУРЕНТНОСТИ...
   (WaitGroup, Semaphore, RateLimiter, LoadBalancer, Fan-in/Fan-out)
================================================================================

🕐 1️⃣  Какое сейчас локальное время у пассажира?
   ----------------------------------------------------------------------------
   🕐 Локальное время: 15:00 11.10.2025
   🌍 Часовой пояс: Asia/Irkutsk

🏁 2️⃣  На какой станции пассажир сейчас находится?
   ----------------------------------------------------------------------------
   🚉 Станция: Хушенга
   📏 Расстояние от Москвы: 6528 км

[... остальные вопросы ...]
```

## 📚 Образовательная ценность

### Для изучающих Go

Этот проект демонстрирует:

1. **Работу с временем и часовыми поясами**
   - Парсинг времени из JSON
   - Конвертация между временными зонами
   - Расчёты с учётом смены дней

2. **Структурирование больших проектов**
   - Разделение на пакеты (internal structure)
   - Интерфейсы и абстракции
   - Dependency injection

3. **Конкурентное программирование**
   - Безопасная работа с shared state
   - Паттерны синхронизации
   - Каналы и горутины

4. **Алгоритмы и структуры данных**
   - Практическое применение классических алгоритмов
   - Оптимизация производительности
   - Выбор правильной структуры данных

5. **Generics в Go**
   - Параметризованные типы
   - Type constraints
   - Реальное применение generics

## 🛠️ Технические детали

### Расстояния

Расстояния между станциями рассчитаны на основе реальных данных Транссибирской магистрали:
- Москва → Хабаровск: ~9104 км
- Средняя скорость поезда: ~90 км/ч
- Время в пути: ~4.5 дня

### Обработка времени

Программа корректно обрабатывает:
- Смену дней (если время прибытия < времени отправления)
- Переход между часовыми поясами
- Локальное время на каждой станции
- Расчёт времени в пути

### Кэширование

Кэш использует TTL (Time To Live) для автоматической очистки:
- Позиции кэшируются на 1 минуту
- Расчёты расстояний кэшируются на 5 минут
- Автоматическая очистка устаревших записей

## 🔧 Возможные улучшения

1. **HTTP API сервер**
   - REST endpoints для каждого вопроса
   - WebSocket для real-time обновлений
   - Swagger документация

2. **Веб-интерфейс**
   - Интерактивная карта маршрута
   - Визуализация движения поезда
   - Графики скорости и расстояний

3. **База данных**
   - Хранение истории позиций
   - Статистика путешествий
   - Персистентный кэш

4. **Мобильное приложение**
   - Push-уведомления о приближении к станциям
   - Оффлайн режим
   - Геолокация

5. **Тестирование**
   - Unit tests для всех компонентов
   - Integration tests
   - Benchmark tests для алгоритмов

6. **Мониторинг**
   - Prometheus метрики
   - Grafana дашборды
   - Distributed tracing

## 📝 Лицензия

Этот проект создан в образовательных целях.

## 🤝 Контрибьюторы

Создано с ❤️ для Рейны и всех, кто хочет изучить продвинутый Go!

## 📧 Контакты

Если у вас есть вопросы или предложения, создайте issue в репозитории.

---

**Happy Coding! 🚀**
